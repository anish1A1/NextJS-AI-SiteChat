We used NextJS framework to build our app.

We are creating a AI chat application which has a session when when the session is over it automatically destroys the chats between the user. 

We first used nanoid to use unique value when storing id. We then used ElysiaJS as the backend to keep our code clean.

ElysiaJS --> We are using ElysiaJS for our backend because is extremely fast and handles raw API and database queries significantly faster then NextJS as it uses bun package manager (but still we did the project in npm) 

It is Designed with simplicity and type-safety in mind, Elysia offers a familiar API with extensive support for TypeScript and is optimized for Bun.

It is just like FastAPI python web framework. fast and ready to start building application.


Eden --> Then to keep our api data's type-safe we used Eden. We installed the eden package which works really well with the Elysia. It validate data in runtime.

Eden gives the frontend full type-awareness of the backend API without any manual code generation. If I modify an API schema on the backend, TypeScript immediately catches breaking changes on the Next.js frontend, preventing runtime bugs before they hit production.

TanStack Query --> To maintain background data fetching, caching data, synchronize data with the UI and update server state, we are using Tanstack Query because it simplifies how we can use them.

It comes with all the features for data fetching, automatic caching, smart background refetching and many more.


Upstash Redis --> To store our data in database we used upstash redis which stores data in key value pair format. It provides serverless, HTTP-based Redis that scales to zero when idle and avoids traditional TCP connection limit (like normal redis).




We first created the home page which has a create chat button. Then when the button is clicked it will take to a room of that particular id, where a new chat gets created it has automatic destroy after a particular time is hit and all the data will be gone. It has minimilastic UI. 

usually  user --> room
but we made, user --> proxy --> room

When the user creates the chat he/she is routed to room/{generated_roomId}. The proxy.ts will check the authorization of the user. From proxy The user will be added to to redis db, a cookie will be set using token(used uuid to genrate it). This room db will have a connected:list and createdAt:Timestamp. If there is no roomId that is equal to current uri after room/{id}  searched from db than the user will be redirected to / (home) page.
Here proxy will check if the current user has already been joined in this cuurent chat by looking the cookie, and comparing it with the connected list from the db. -> (1) If user exists than rejoin, 

(2) if chat users (connected:listof db) length is more than or equal 2 redirect to home page (since new user is joining while existing 2 users)    

(3) Add the current user with the token and set the cookie with the help of new generated token, then add his token to the connected:lists of the redis db.



After that we then created a authMiddleware in the ElysiaJS (backend) to validate the users who are trying to send or view message. 
api/[[...slugs]]/auth.js

We first created a resuable AuthError, this AuthError will be used to provide the message of the Error.
Then we created authMiddleware which uses AuthError, it first check if user is Unauthorized. Then it checks each time if there is Missing roomId, token and also compares if connected:list is equal to the token. If yes it throw AuthError displaying error for each error types. And if everything is Ok then it returns {rommId, token, and connected:list} inside auth.


We created post request for messages, where we used authMiddleware first then used post method and done validation. We also used zod library to define our data's schema and also added validation to incoming datas with it.



Then we install upstash/realtime to work with realtime subscribing events. just like chat application

After that we created a schema in realtime.js and wrote all the schema for the message and chat. in chat we also destroy object, so when a chat is destroyed we will know about that.

Then we wrapped the RealTimeProvider above the Tanstack QueryClientProviders 
Then we added a realtime-client.ts to check the events that gonna happen.

